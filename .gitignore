/.gradle/
/.idea/
/local.properties
**/build/
*.iml

# Android build outputs & signing (added for public release)
*.apk
*.aab
*.keystore
*.jks
captures/

# 软件使用手册（仅本地保留，不公开到仓库）
软件使用手册.*

# 开发日志（仅本地保留，不公开到仓库）
BUGLOG.md
