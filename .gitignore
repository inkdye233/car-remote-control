/.gradle/
/.idea/
/local.properties
**/build/
*.iml

# Android build outputs & signing (added for public release)
*.apk
*.aab
*.keystore
*.jks
captures/

# 软件使用手册（仅本地保留，不公开到仓库）
软件使用手册.*

# 开发日志（仅本地保留，不公开到仓库）
BUGLOG.md

# 本机构建辅助脚本（硬编码本机 JDK/SDK 路径，仅本地使用）
build-local.bat
